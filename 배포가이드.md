# 온누리한의원 홈페이지 배포 가이드

> **목표**: 가비아에서 산 도메인 `onnuriclinic.com`으로 한의원 홈페이지 + AI 챗봇이 정상 작동하도록 배포.
>
> 전체 흐름: **GitHub(코드 보관) → Supabase(챗봇 백엔드) → Vercel(웹 호스팅) → 가비아(도메인 연결)**

---

## 0단계 · 준비물 체크

| 항목 | 필요 여부 | 확인 |
|---|---|---|
| GitHub 계정 | 필요 | github.com에서 가입 |
| Vercel 계정 | 필요 | vercel.com → "Continue with GitHub"로 가입 권장 |
| Supabase 계정 | 필요 | supabase.com → "Sign in with GitHub"로 가입 권장 |
| 가비아 로그인 | 필요 | onnuriclinic.com 등록한 계정 |
| Anthropic API 키 | 필요 | console.anthropic.com → API Keys → Create Key |

> **모든 가입은 GitHub 연동을 추천**합니다. 비밀번호 관리가 줄어들고 연결이 매끄럽습니다.

---

## 1단계 · GitHub에 저장소 만들기

1. https://github.com/new 접속
2. Repository name: `onnuriclinic-web`
3. **Public** 또는 **Private** 둘 다 OK (Vercel은 둘 다 지원)
4. "Add a README file" 체크 해제 (이미 README.md가 있음)
5. **Create repository** 클릭

### 파일 업로드 (가장 쉬운 방법: 웹 업로드)

1. 만든 저장소 페이지에서 **uploading an existing file** 링크 클릭
2. `C:\Users\ADmiN\Documents\한의원\웹싸이트` 폴더 안의 모든 파일/폴더를 드래그앤드롭
   - `onnuri_clinic (3)_mobile.html`
   - `vercel.json`
   - `README.md`
   - `.gitignore`
   - `supabase/` 폴더 통째로
   - `배포가이드.md`
3. Commit changes 클릭

> 폴더 구조가 그대로 올라가야 합니다. `supabase/functions/chat/index.ts` 경로가 그대로 유지되어야 함.

---

## 2단계 · Supabase 프로젝트 만들기

1. https://supabase.com/dashboard 접속
2. **New project** 클릭
3. 입력:
   - Organization: 본인 이름 또는 "Onnuri"
   - Name: `onnuriclinic`
   - Database Password: **자동 생성된 강력한 비밀번호 사용 + 안전한 곳에 저장**
   - Region: **Northeast Asia (Seoul)** 선택
   - Plan: **Free** 충분
4. **Create new project** 클릭 (생성에 1~2분 소요)

### 2-1) 프로젝트 ID 메모

Settings → General → **Reference ID** 항목의 값을 복사.
예시: `abcdefghijklmnop` → 이 값이 `vercel.json`에 들어갈 ID.

### 2-2) 환경변수에 Anthropic API 키 등록

1. 좌측 메뉴 **Project Settings → Edge Functions → Secrets**
2. **Add new secret** 클릭
3. Name: `ANTHROPIC_API_KEY` / Value: 본인의 Anthropic API 키 (sk-ant-... 로 시작)
4. **Save** 클릭

### 2-3) Edge Function 배포 (chat)

1. 좌측 메뉴 **Edge Functions** → **Deploy a new function** 클릭
2. Function name: `chat`
3. **Verify JWT with legacy secret** 옵션 → **체크 해제** (공개 챗봇이므로)
4. 코드 입력란에 `supabase/functions/chat/index.ts` 파일 내용 전체를 복사해서 붙여넣기
5. **Deploy** 클릭
6. 배포되면 Function URL이 표시됨:
   `https://YOUR_PROJECT_REF.supabase.co/functions/v1/chat`
   → 이 URL을 메모

### 2-4) DB 테이블 생성 (예약/문의)

1. 좌측 메뉴 **SQL Editor** → **New query**
2. `supabase/migrations/001_inquiries.sql` 파일 내용 전체를 복사해서 붙여넣기
3. **Run** 클릭
4. "Success. No rows returned" 메시지 확인

---

## 3단계 · vercel.json 수정 후 GitHub에 반영

1. GitHub 저장소에서 `vercel.json` 클릭 → 연필(✏️) 아이콘으로 편집
2. `YOUR_SUPABASE_PROJECT_REF` 부분을 2-1)에서 메모한 본인 프로젝트 ID로 교체
   ```json
   { "source": "/api/chat", "destination": "https://abcdefghijklmnop.supabase.co/functions/v1/chat" }
   ```
3. **Commit changes** 클릭

---

## 4단계 · Vercel에 배포

1. https://vercel.com/new 접속
2. **Import Git Repository** → `onnuriclinic-web` 선택 → **Import**
3. 설정:
   - Framework Preset: **Other** (자동 감지될 것)
   - Root Directory: `./` (그대로)
   - Build/Output 설정: 건드리지 말 것 (정적 사이트)
4. **Deploy** 클릭
5. 1~2분 기다리면 `https://onnuriclinic-web.vercel.app` 같은 URL이 생김
6. 그 URL로 접속해서 사이트가 뜨는지, 챗봇이 작동하는지 확인

> 챗봇이 안 되면? `vercel.json`의 Supabase URL이 정확한지, Supabase 환경변수 `ANTHROPIC_API_KEY`가 등록됐는지 확인.

---

## 5단계 · Vercel에 도메인 추가

1. Vercel 프로젝트 페이지 → 상단 **Settings → Domains**
2. 입력란에 `onnuriclinic.com` 입력 → **Add**
3. 다시 입력란에 `www.onnuriclinic.com` 입력 → **Add**
4. Vercel이 표시하는 DNS 안내 화면을 그대로 둔 채 다음 단계로 이동
   - 보통 두 가지 형태로 안내됨:
     - **A 레코드**: 호스트 `@` → IP `76.76.21.21`
     - **CNAME 레코드**: 호스트 `www` → 값 `cname.vercel-dns.com`
   - 이 값들을 메모(또는 화면을 켜둔 채)

---

## 6단계 · 가비아 DNS 설정

1. https://my.gabia.com 로그인
2. 상단 **My가비아 → 도메인** 클릭
3. `onnuriclinic.com` 우측 **관리** 버튼
4. 좌측 메뉴 **DNS 관리툴** → **DNS 설정** 클릭
5. 기존 레코드 중 **A 레코드 / CNAME 레코드** 가 가비아 기본값으로 있다면 모두 수정/삭제
6. 다음 두 줄을 추가:

   | 타입 | 호스트 | 값/위치 | TTL |
   |---|---|---|---|
   | A | @ | 76.76.21.21 | 3600 |
   | CNAME | www | cname.vercel-dns.com. | 3600 |

   > CNAME 값 끝에 `.` (점) 빠지면 가비아가 자동으로 붙여줌. 헷갈리면 그대로 입력해도 됨.
7. **저장(확인)** 클릭

> DNS 전파에 보통 5분~1시간(드물게 24시간) 걸림.

---

## 7단계 · Vercel에서 도메인 검증

1. Vercel **Settings → Domains** 페이지로 돌아오기
2. `onnuriclinic.com` 옆 **Refresh** 버튼 클릭
3. 잠시 후 ✓ Valid Configuration 표시되면 완료
4. https://onnuriclinic.com 접속해서 사이트 확인
5. SSL(자물쇠 아이콘) 자동 발급됨 (Vercel이 Let's Encrypt로 처리)

---

## 8단계 · 최종 점검

- [ ] https://onnuriclinic.com 접속 시 한의원 사이트가 뜬다
- [ ] https://www.onnuriclinic.com 도 동일하게 뜬다
- [ ] 자물쇠(HTTPS) 아이콘이 정상이다
- [ ] 모바일에서 봐도 디자인이 깨지지 않는다
- [ ] AI 챗봇에 메시지 입력 시 응답이 온다 (실패 시 → Supabase 로그 확인)
- [ ] 전화 예약 버튼(📞 031-223-1075)이 작동한다

---

## 자주 막히는 곳

**Q. 챗봇이 "일시적인 오류가 발생했습니다"라고만 응답해요**
→ 다음 순서로 확인:
1. 브라우저 개발자도구(F12) → Network 탭에서 `/api/chat` 호출 결과 확인
2. 401/403 → Supabase Edge Function이 "Verify JWT" 옵션 켜진 상태로 배포됨. 옵션 해제하고 재배포
3. 500 → Supabase Dashboard → Edge Functions → `chat` → Logs에서 실제 오류 확인
4. CORS 오류 → Edge Function 코드의 `ALLOWED_ORIGINS`에 도메인 추가됐는지 확인

**Q. 도메인 접속 시 "DNS_PROBE_FINISHED_NXDOMAIN" 오류**
→ 가비아 DNS 설정 후 보통 30분 정도 기다려야 합니다. `nslookup onnuriclinic.com` 명령으로 IP가 76.76.21.21로 잡히면 정상.

**Q. Vercel에서 "Invalid Configuration" 에러**
→ 가비아 DNS 레코드의 호스트가 `@`(루트), `www`로 정확히 들어갔는지 확인. 가비아 UI에 따라 호스트 입력 방식이 다를 수 있음.

**Q. API 키 보안이 걱정됩니다**
→ `ANTHROPIC_API_KEY`는 Supabase 서버에서만 사용되고, 사용자 브라우저에는 절대 노출되지 않습니다. 추가 보호로:
- Anthropic Console에서 해당 키에 사용량 한도 설정
- Supabase Edge Function에 Rate Limit 추가 (Pro 플랜 필요)

---

## 운영 팁

- **콘텐츠 수정**: GitHub에서 `onnuri_clinic (3)_mobile.html` 편집 → Commit → Vercel이 자동으로 재배포
- **문의 데이터 보기**: Supabase Dashboard → Table Editor → `inquiries` 테이블
- **챗봇 시스템 프롬프트 수정**: HTML 파일 안 `SYSTEM_PROMPT` 변수 편집 후 Commit
- **트래픽 통계**: Vercel Dashboard → Analytics

---

배포 중 막히는 부분이 있으면 어디까지 진행했는지 알려주세요.
같이 확인하면서 도와드리겠습니다.
